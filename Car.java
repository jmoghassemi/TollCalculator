package main;

// The Car class which implements the getFee() method from the Vehicle Interface, and provides a specific definition for that method.

public class Car implements Vehicle {
    public int getFee() {
        return 11;
    }
}
