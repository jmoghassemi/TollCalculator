package main.TollCalculator;

//The Vehicle Interface declaring the getFee() method, which is later uniquely defined by the Car and MotorBike classes.
public interface Vehicle {
    int getFee();
}
